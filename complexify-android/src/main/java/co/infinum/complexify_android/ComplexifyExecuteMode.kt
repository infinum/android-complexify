package co.infinum.complexify_android

/**
 * Enum for execution mode.
 *
 * SYNC - execution is synchronous
 * ASYNC - execution is asynchronous
 */
enum class ComplexifyExecuteMode {
    SYNC, ASYNC
}
