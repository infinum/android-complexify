buildscript {

    ext.buildConfig = [
        "minSdk"    : 21,
        "compileSdk": 30,
        "targetSdk" : 30
    ]

    apply from: "dependencies.gradle"

    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath packages.gradle

        classpath packages.bintray
        classpath packages.maven
    }
}

allprojects {
    repositories {
        jcenter()
        google()
    }
}
